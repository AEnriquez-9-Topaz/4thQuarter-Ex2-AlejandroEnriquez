function verifyAge () {

	// If statement

		// let age = document.getElementById('inputAge').value;
		// if (age<18 && age>0){window.alert("You are not eligible to vote")};
		// if (age>17 && age<120) {window.alert("You are eligable to vote")}

	// If-else statement
	let age = document.getElementById('inputAge').value;
	if (age<18){window.alert("You are not eligible to vote")}
	else {window.alert("You are eligable to vote")}
		}	
 

						
